# Tiêu chuẩn Đặt tên (Naming Conventions)

- **Thư mục & File Component (React):** Sử dụng `PascalCase`. 
  - *Ví dụ:* `OrderMenu.tsx`, `TableMap.tsx`, `KitchenTicket.tsx`.
- **Hàm (Functions) & Biến (Variables):** Sử dụng `camelCase`.
  - *Ví dụ:* `calculateTotal()`, `currentOrder`.
- **Kiểu dữ liệu (Types/Interfaces):** Sử dụng `PascalCase` và có thể thêm tiền tố `I` hoặc hậu tố `Type`.
  - *Ví dụ:* `IProduct`, `OrderType`.
- **Hằng số (Constants):** Sử dụng `UPPER_SNAKE_CASE`.
  - *Ví dụ:* `MAX_TABLE_CAPACITY`, `API_BASE_URL`.
